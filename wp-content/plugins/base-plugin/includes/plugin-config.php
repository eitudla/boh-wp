<?php

/**
 * Site Prefix
 */
if( !defined('BASE_SITE_PREFIX') ) {
    define( 'BASE_SITE_PREFIX', 'base-' );
}

/**
 * Plugin Directory
 */
if( !defined('BASE_PLUGIN_DIR') ) {
    define( 'BASE_PLUGIN_DIR', dirname( __FILE__ ) . '/..' );
}

/**
 * Site Prefix
 */
if( !defined('BASE_SLUG') ) {
    define( 'BASE_SLUG', 'base-plugin' );
}

/**
 * Event Options name in cms settings
 */
if( !defined( 'BASE_OPTIONS_NAME' ) ) {
    define( 'BASE_OPTIONS_NAME', 'base_options_name' );
}